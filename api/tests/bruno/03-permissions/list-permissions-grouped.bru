meta {
  name: List permissions grouped
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/permissions/grouped
  body: none
  auth: bearer
}

auth:bearer {
  token: {{testAdminToken}}
}

assert {
  res.status: eq 200
}
