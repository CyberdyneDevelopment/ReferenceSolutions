meta {
  name: Delete TestApiRole
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/roles/TestApiRole
  body: none
  auth: bearer
}

auth:bearer {
  token: {{testAdminToken}}
}

assert {
  res.status: eq 200
}
