meta {
  name: Get Admin role
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/roles/Admin
  body: none
  auth: bearer
}

auth:bearer {
  token: {{testAdminToken}}
}

assert {
  res.status: eq 200
}
