meta {
  name: Create user (no token)
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: none
}

body:json {
  {
    "Username": "shouldFail",
    "Password": "Test123#",
    "Email": "fail@test.com"
  }
}

assert {
  res.status: eq 401
}
