meta {
  name: Create connection (no token)
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/connections
  body: json
  auth: none
}

body:json {
  {
    "Name": "ShouldFail"
  }
}

assert {
  res.status: eq 401
}
