meta {
  name: List datastores
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/datastores
  body: none
  auth: bearer
}

auth:bearer {
  token: {{testAdminToken}}
}

assert {
  res.status: eq 200
}
