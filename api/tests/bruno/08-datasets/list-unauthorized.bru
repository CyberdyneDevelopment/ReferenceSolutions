meta {
  name: List datasets (unauthorized)
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/datasets
  body: none
  auth: bearer
}

auth:bearer {
  token: {{testUnauthorizedToken}}
}

assert {
  res.status: in [401, 403]
}
