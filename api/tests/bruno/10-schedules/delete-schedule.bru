meta {
  name: Delete TestApiSchedule
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/schedules/TestApiSchedule
  body: none
  auth: bearer
}

auth:bearer {
  token: {{testAdminToken}}
}

assert {
  res.status: eq 200
}
