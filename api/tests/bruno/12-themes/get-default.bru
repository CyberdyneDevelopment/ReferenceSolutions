meta {
  name: Get default theme
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/themes/default
  body: none
  auth: bearer
}

auth:bearer {
  token: {{testAdminToken}}
}

assert {
  res.status: in [200, 404]
}
