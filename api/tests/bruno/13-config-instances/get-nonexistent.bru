meta {
  name: Get non-existent config instance
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/configuration/instances/Connection/NoSuchInstance
  body: none
  auth: bearer
}

auth:bearer {
  token: {{testAdminToken}}
}

assert {
  res.status: eq 404
}
