meta {
  name: List promotion requests
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/promotion/requests
  body: none
  auth: bearer
}

auth:bearer {
  token: {{testAdminToken}}
}

assert {
  res.status: eq 200
}
